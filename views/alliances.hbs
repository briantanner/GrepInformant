<h3>{{title}}</h3>

<div class="row">
  <div class="col-md-12">
    {{>pagination}}

    <table class="table table-striped">
      <tr>
        <th>Rank</th>
        <th>Name</th>
        <th>Members</th>
        <th>Towns</th>
        <th>Points</th>
        <th>ABP</th>
        <th>DBP</th>
      </tr>
      {{#each alliances}}
        <tr>
          <td>{{rank}}</td>
          <td>
            <a href="/{{../server}}/alliance/{{id}}" title="{{name}}">{{name}}</a>
          </td>
          <td>{{members}}</td>
          <td>{{towns}}</td>
          <td>{{points}}</td>
          <td>{{abp}}</td>
          <td>{{dbp}}</td>
        </tr>
      {{/each}}
    </table>

    {{>pagination}}
  </div>
</div>